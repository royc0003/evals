benchmark: aime
harness: lm-eval-harness  # record the exact version on the first run
task: aime2025  # switch to the 2026 task once the harness ships it
generation:
  temperature: 0.6
  top_p: 0.95
  max_gen_toks: 32768
num_concurrent: 8
reference:
  glm_5_2_reported: 99.2  # AIME 2026, graded by GPT-5.5 as judge
notes: >-
  No LLM judge locally: rule-based extraction is stricter than the
  judge used in the GLM-5.2 run. Long reasoning generations dominate
  runtime; max_gen_toks must fit inside the server's --max-model-len.
