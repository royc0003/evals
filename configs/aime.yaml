# Primary reasoning eval: 30 competition math problems, public dataset,
# no gating, rule-based numeric answer matching. Same exam year as the
# GLM-5.2 blogpost.
benchmark: aime
harness: lm-eval-harness  # record the exact version on the first run
task: aime26  # repo-local task in tasks/aime26/, via --include_path
include_path: tasks/aime26
generation:
  temperature: 0.6
  top_p: 0.95
  max_gen_toks: 32768
num_concurrent: 8
reference:
  glm_5_2_reported: 99.2  # AIME 2026, graded by GPT-5.5 as judge
notes: >-
  Dataset math-ai/aime26; task definition mirrors the harness's
  built-in aime25, plus an explicit "put your final answer within
  boxed{}" instruction (without it the model answers unboxed and the
  grader scores 0). Grading differs from the GLM-5.2 run: they used
  GPT-5.5 as an answer judge, we use rule-based boxed-answer
  extraction, which is stricter (correct-but-unusually-formatted
  answers score 0 for us that a judge would accept).
